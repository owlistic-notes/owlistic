document.addEventListener("DOMContentLoaded", () => {
  const appDiv = document.getElementById("app");
  appDiv.innerHTML = `
    <h2>Dashboard</h2>
    <p>Welcome to the ThinkStack application. Use the navigation menu to explore.</p>
  `;
});
